# Base URL of the Commerce API, including the version prefix.
#
# The mock API (`npm run mock:dev`) and the real NestJS API (`npm run api:dev`)
# both serve this address, so switching between them needs no change here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
