/// The product's name, in one place.
///
/// The code base is still called "commerce" (the monorepo, the API, the
/// package name); what customers and sellers see is this.
class AppBrand {
  const AppBrand._();

  static const name = 'Good for Goods';

  /// Who makes it — shown on the splash screen.
  static const maker = 'iZyane';
}
